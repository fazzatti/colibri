[package]
name = "executable-ref-manager-contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = "=28.0.0-rc.1"

[dev-dependencies]
soroban-sdk = { version = "=28.0.0-rc.1", features = ["testutils"] }
