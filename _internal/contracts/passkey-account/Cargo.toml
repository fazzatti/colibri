[package]
name = "passkey-account-contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
p256 = { version = "0.13.2", features = ["ecdsa"] }
soroban-sdk = { workspace = true, features = ["testutils"] }
