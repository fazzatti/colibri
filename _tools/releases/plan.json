{
  "schema": 1,
  "base": "2a5ded08d6cac7ae452c083468575d1f70a20171",
  "packages": {
    "@colibri/core": {
      "bump": "patch",
      "reason": "Standardize internal error namespace imports as ERROR and align error references. Document CAP-40 signed-payload signer nomenclature.",
      "apiReview": "Local namespace aliases change from E to ERROR. Existing exported names, constructor identities, signatures, error codes and behavior are preserved. The declaration snapshot changes only local namespace qualifiers; no API additions or removals. Existing Core dependency ranges remain sufficient."
    },
    "@colibri/plugin-fee-bump": {
      "bump": "patch",
      "reason": "Standardize internal error namespace imports as ERROR and align error references.",
      "apiReview": "Local namespace aliases change from E to ERROR. Existing exported names, constructor identities, signatures, error codes and behavior are preserved. The declaration snapshot changes only local namespace qualifiers; no API additions or removals. Existing Core dependency ranges remain sufficient."
    },
    "@colibri/plugin-channel-accounts": {
      "bump": "patch",
      "reason": "Standardize internal error namespace imports as ERROR and align error references.",
      "apiReview": "Local namespace aliases change from E to ERROR. Existing exported names, constructor identities, signatures, error codes and behavior are preserved. The declaration snapshot changes only local namespace qualifiers; no API additions or removals. Existing Core dependency ranges remain sufficient."
    },
    "@colibri/plugin-sep29": {
      "bump": "patch",
      "reason": "Standardize internal error namespace imports as ERROR and align error references.",
      "apiReview": "Local namespace aliases change from E to ERROR. Existing exported names, constructor identities, signatures, error codes and behavior are preserved. The declaration snapshot changes only local namespace qualifiers; no API additions or removals. Existing Core dependency ranges remain sufficient."
    }
  }
}
