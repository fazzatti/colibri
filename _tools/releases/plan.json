{
  "schema": 1,
  "base": "7c12c47bf5d5a5b0ee757ea31fc0aaef06d5c0c1",
  "packages": {
    "@colibri/react": {
      "bump": "minor",
      "reason": "Support consumer migrations with explicit wallet authorization entries, optional async clients, structural client identity and shared query utilities.",
      "apiReview": "Additive APIs preserve current hooks and explicit signing policies. Generated client identity no longer depends on private Core class fields. ABI keys use full-content SHA-256 fingerprints; disabled clients cannot fetch. Wallet authorization validates address, network, expiry and payload preservation. Token precision reads share a five-minute cache."
    }
  }
}
