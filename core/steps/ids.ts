/** Stable step identifier for assemble-transaction pipeline steps. */
export const ASSEMBLE_TRANSACTION_STEP_ID = "assemble-transaction" as const;
/** Stable step identifier for build-transaction pipeline steps. */
export const BUILD_TRANSACTION_STEP_ID = "build-transaction" as const;
/** Stable step identifier for envelope-signing-requirements steps. */
export const ENVELOPE_SIGNING_REQUIREMENTS_STEP_ID =
  "envelope-signing-requirements" as const;
/** Stable step identifier for send-transaction pipeline steps. */
export const SEND_TRANSACTION_STEP_ID = "send-transaction" as const;
/** Stable step identifier for sign-auth-entries pipeline steps. */
export const SIGN_AUTH_ENTRIES_STEP_ID = "sign-auth-entries" as const;
/** Stable step identifier for sign-envelope pipeline steps. */
export const SIGN_ENVELOPE_STEP_ID = "sign-envelope" as const;
/** Stable step identifier for simulate-transaction pipeline steps. */
export const SIMULATE_TRANSACTION_STEP_ID = "simulate-transaction" as const;
/** Stable step identifier for wrap-fee-bump pipeline steps. */
export const WRAP_FEE_BUMP_STEP_ID = "wrap-fee-bump" as const;
